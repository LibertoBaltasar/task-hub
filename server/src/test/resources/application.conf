ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ org.taskhub.server.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:h2:mem:taskhub_test;DB_CLOSE_DELAY=-1"
    url = ${?DATABASE_URL}
    user = "sa"
    user = ${?DATABASE_USER}
    password = ""
    password = ${?DATABASE_PASSWORD}
}